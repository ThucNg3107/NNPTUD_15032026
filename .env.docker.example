# Copy to .env.docker before running docker compose
MONGO_URI=mongodb://mongo:27017/NNPTUD-C3

# Use local image tags to avoid pulling from Docker Hub.
# Ensure these images already exist locally.
APP_IMAGE=nnptud-c3-app:local
APP_BASE_IMAGE=node:20-alpine
MONGO_IMAGE=mongo:7
